// js/online_leaderboards.js
import {
  ONLINE_LEADERBOARD_DIFFICULTIES,
  ONLINE_LEADERBOARD_MAPS,
  ONLINE_LEADERBOARD_WORKER_URL,
  buildOnlineChallenge,
  buildOnlineSubmission,
  cleanOnlineDisplayName,
  createOnlineRunId,
  normalizeOnlineDifficulty,
  normalizeOnlineLeaderboardResponse,
  normalizeOnlineMap,
  normalizePendingOnlineSubmissions
} from './online_leaderboards_core.js';

const PLAYER_ID_KEY = 'ka_online_leaderboard_player_v1';
const DISPLAY_NAME_KEY = 'ka_online_leaderboard_name_v1';
const PENDING_KEY = 'ka_online_leaderboard_pending_v1';
const REQUEST_TIMEOUT_MS = 7000;
let activeRun = null;
let uiBound = false;
let latest = null;

function readStorage(key, fallback='') { try { return localStorage.getItem(key) ?? fallback; } catch { return fallback; } }
function writeStorage(key,value) { try { localStorage.setItem(key,value); } catch {} }
function playerId() { let value=readStorage(PLAYER_ID_KEY); if(!/^[a-zA-Z0-9:_-]{8,120}$/.test(value)){ value=`player-${crypto.randomUUID()}`; writeStorage(PLAYER_ID_KEY,value); } return value; }
function displayName() { const fallback=`Survivor-${playerId().slice(-4).toUpperCase()}`; return cleanOnlineDisplayName(readStorage(DISPLAY_NAME_KEY,fallback)); }
function numberFrom(source, paths, fallback=0) { for(const path of paths){ let value=source; for(const part of path.split('.')) value=value?.[part]; const number=Number(value); if(Number.isFinite(number)) return number; } return fallback; }
function make(tag,attrs={},text=''){ const el=document.createElement(tag); for(const [key,value] of Object.entries(attrs)){ if(key==='class') el.className=value; else el.setAttribute(key,String(value)); } if(text) el.textContent=text; return el; }
function formatTime(seconds){ const total=Math.max(0,Math.round(Number(seconds)||0)); return `${Math.floor(total/60)}:${String(total%60).padStart(2,'0')}`; }
function setStatus(text,tone=''){ const el=document.getElementById('ka-online-lb-status'); if(el){ el.textContent=text; el.dataset.tone=tone; } document.documentElement.dataset.kaOnlineLeaderboardStatus=tone||'idle'; }
async function requestJson(path,{method='GET',body=null}={}){ const controller=new AbortController(); const timer=setTimeout(()=>controller.abort(),REQUEST_TIMEOUT_MS); try{ const response=await fetch(`${ONLINE_LEADERBOARD_WORKER_URL}${path}`,{method,cache:'no-store',credentials:'omit',headers:body?{'content-type':'application/json'}:undefined,body:body?JSON.stringify(body):undefined,signal:controller.signal}); const value=await response.json().catch(()=>({})); if(!response.ok||value.ok!==true) throw new Error(String(value.error||`HTTP_${response.status}`)); return value; } finally { clearTimeout(timer); } }
async function issueChallenge(run){ const value=await requestJson('/leaderboards/challenge',{method:'POST',body:buildOnlineChallenge(run)}); run.challengeToken=String(value.challengeToken||''); run.challengeExpiresAt=Date.parse(value.expiresAt)||0; run.region=String(value.region||'ZZ'); return run; }
function pending(){ try{return normalizePendingOnlineSubmissions(JSON.parse(readStorage(PENDING_KEY,'[]')));}catch{return normalizePendingOnlineSubmissions([]);} }
function savePending(items){ writeStorage(PENDING_KEY,JSON.stringify(normalizePendingOnlineSubmissions(items))); updatePendingCount(); }
function queueSubmission(item){ const items=pending().filter((entry)=>entry.runId!==item.runId); items.push({...item,queuedAt:new Date().toISOString()}); savePending(items); }
function updatePendingCount(){ const el=document.getElementById('ka-online-lb-pending'); if(el) el.textContent=String(pending().length); }

export function beginOnlineLeaderboardRun({mapId,difficulty,mode='single'}={}){
  activeRun={playerId:playerId(),runId:createOnlineRunId(),mapId:normalizeOnlineMap(mapId),difficulty:normalizeOnlineDifficulty(difficulty),mode:String(mode).toLowerCase(),challengeToken:'',challengeExpiresAt:0,region:'ZZ'};
  if(activeRun.mode!=='single') return Promise.resolve({accepted:false,reason:'MULTIPLAYER_EXCLUDED'});
  activeRun.challengePromise=issueChallenge(activeRun).then(()=>({accepted:true,runId:activeRun.runId})).catch((error)=>({accepted:false,reason:String(error.message||error)}));
  return activeRun.challengePromise;
}

async function submitPayload(payload,{queueOnFailure=true}={}){
  try{
    let run={...payload};
    if(!run.challengeToken||Number(run.challengeExpiresAt||0)<=Date.now()+5000) run=await issueChallenge(run);
    const value=await requestJson('/leaderboards/submit',{method:'POST',body:buildOnlineSubmission(run)});
    document.documentElement.dataset.kaOnlineLeaderboardLastRank=String(value.globalRank??'outside-top-100');
    setStatus(`ONLINE SCORE ACCEPTED · GLOBAL #${value.globalRank??'—'} · REGION #${value.regionRank??'—'}`,'pass');
    return Object.freeze({accepted:true,...value});
  }catch(error){ if(queueOnFailure) queueSubmission(payload); setStatus(`ONLINE SUBMISSION QUEUED · ${String(error.message||error)}`,'offline'); return Object.freeze({accepted:false,queued:queueOnFailure,reason:String(error.message||error)}); }
}

export function submitOnlineLeaderboardRun({mapId,difficulty,score=0,wave=1,kills=0,summary=null,mode='single'}={}){
  if(String(mode).toLowerCase()!=='single') return Promise.resolve(Object.freeze({accepted:false,reason:'MULTIPLAYER_EXCLUDED'}));
  if(!activeRun||activeRun.mode!=='single') beginOnlineLeaderboardRun({mapId,difficulty,mode});
  const source=summary&&typeof summary==='object'?summary:{};
  const payload={...activeRun,displayName:displayName(),mapId:normalizeOnlineMap(mapId),difficulty:normalizeOnlineDifficulty(difficulty),score:numberFrom(source,['score','finalScore','combat.score'],score),wave:numberFrom(source,['wave','finalWave','combat.wave'],wave),kills:numberFrom(source,['kills','eliminations','combat.kills'],kills),survivalSeconds:numberFrom(source,['survivalSeconds','survivalTimeSeconds','durationSeconds','timeSeconds','combat.survivalSeconds'],0),accuracy:numberFrom(source,['accuracy','accuracyPercent','combat.accuracy'],0),headshots:numberFrom(source,['headshots','headshotKills','combat.headshots'],0)};
  return Promise.resolve(activeRun?.challengePromise).catch(()=>null).then(()=>submitPayload({...payload,...activeRun}));
}

async function retryPending(){ const items=[...pending()]; if(!items.length){setStatus('NO QUEUED SUBMISSIONS','pass');return;} setStatus(`RETRYING ${items.length} SUBMISSION${items.length===1?'':'S'}…`,'loading'); const remaining=[]; for(const item of items){ const result=await submitPayload({...item,challengeToken:'',challengeExpiresAt:0},{queueOnFailure:false}); if(!result.accepted) remaining.push(item); } savePending(remaining); setStatus(remaining.length?`${remaining.length} SUBMISSION${remaining.length===1?'':'S'} STILL QUEUED`:'ALL QUEUED SUBMISSIONS SENT',remaining.length?'offline':'pass'); }
async function refresh(){ const mapId=document.getElementById('ka-online-lb-map')?.value||'grid_bunker'; const difficulty=document.getElementById('ka-online-lb-difficulty')?.value||'normal'; const scope=document.getElementById('ka-online-lb-scope')?.value||'global'; setStatus('LOADING ONLINE LEADERBOARD…','loading'); try{ latest=normalizeOnlineLeaderboardResponse(await requestJson(`/leaderboards?scope=${encodeURIComponent(scope)}&mapId=${encodeURIComponent(mapId)}&difficulty=${encodeURIComponent(difficulty)}&limit=10`)); render(); setStatus(`${scope==='region'?`REGION ${latest.region||'—'}`:'GLOBAL'} · UPDATED`,'pass'); }catch(error){ latest=null; render(); setStatus(`ONLINE LEADERBOARD UNAVAILABLE · ${String(error.message||error)}`,'offline'); } }
function render(){ const body=document.getElementById('ka-online-lb-body'); if(!body)return; body.replaceChildren(); const entries=latest?.entries||[]; if(!entries.length){ const row=make('tr'); row.append(make('td',{colspan:'9',class:'ka-online-lb-empty'},latest?'No online scores in this category yet.':'Online leaderboard data is unavailable. Local scores remain available.')); body.append(row); return; } for(const entry of entries){ const row=make('tr'); [entry.rank,entry.displayName,entry.region,entry.score.toLocaleString(),entry.wave,entry.kills,formatTime(entry.survivalSeconds),`${entry.accuracy.toFixed(1)}%`,new Date(entry.createdAt).toLocaleDateString()].forEach((value)=>row.append(make('td',{},String(value)))); body.append(row); } }
function close(){ const dialog=document.getElementById('ka-online-leaderboards-dialog'); if(!dialog)return; if(dialog.contains(document.activeElement))document.activeElement?.blur?.(); if(dialog.open&&dialog.close)dialog.close();else dialog.removeAttribute('open'); queueMicrotask(()=>document.getElementById('ka-online-leaderboards-open')?.focus()); }
function open(){ const dialog=document.getElementById('ka-online-leaderboards-dialog'); if(!dialog)return; if(dialog.showModal)dialog.showModal();else dialog.setAttribute('open',''); document.getElementById('ka-online-lb-name').value=displayName(); updatePendingCount(); void refresh(); requestAnimationFrame(()=>document.getElementById('ka-online-lb-close')?.focus()); }
function buildUi(){ if(document.getElementById('ka-online-leaderboards-dialog'))return; const style=make('style',{id:'ka-online-leaderboards-style'}); style.textContent=`#ka-online-leaderboards-open{margin-top:10px;min-width:220px}#ka-online-leaderboards-dialog{border:1px solid rgba(255,180,52,.62);border-radius:14px;background:rgba(7,10,16,.98);color:#f7fbff;width:min(880px,calc(100vw - 24px));max-height:min(720px,calc(100vh - 24px));padding:0;box-shadow:0 24px 80px rgba(0,0,0,.75)}#ka-online-leaderboards-dialog::backdrop{background:rgba(0,0,0,.78);backdrop-filter:blur(4px)}.ka-online-lb-shell{padding:20px}.ka-online-lb-head{display:flex;justify-content:space-between;gap:12px;align-items:center}.ka-online-lb-head h2{margin:0;color:#ffc66b;letter-spacing:.08em}.ka-online-lb-close{font-size:24px;min-width:44px}.ka-online-lb-profile,.ka-online-lb-filters{display:grid;grid-template-columns:1fr auto auto;gap:10px;margin:14px 0}.ka-online-lb-filters{grid-template-columns:1fr 1fr 1fr auto}.ka-online-lb-profile input,.ka-online-lb-filters select,.ka-online-lb-profile button,.ka-online-lb-filters button{min-height:42px}.ka-online-lb-status{min-height:24px;color:#9fb4c5;font-size:12px}.ka-online-lb-status[data-tone=pass]{color:#7dffb2}.ka-online-lb-status[data-tone=offline]{color:#ffbb6a}.ka-online-lb-table-wrap{overflow:auto;border:1px solid rgba(255,255,255,.12);border-radius:10px}.ka-online-lb-table{width:100%;border-collapse:collapse;min-width:790px}.ka-online-lb-table th,.ka-online-lb-table td{padding:9px;border-bottom:1px solid rgba(255,255,255,.08);text-align:left}.ka-online-lb-table th{position:sticky;top:0;background:#181b22;color:#ffc66b}.ka-online-lb-empty{text-align:center;padding:28px;color:#9fb4c5}.ka-online-lb-note{font-size:12px;color:#9fb4c5}@media(max-width:700px){.ka-online-lb-profile,.ka-online-lb-filters{grid-template-columns:1fr}.ka-online-lb-shell{padding:14px}}`; document.head.append(style); const home=document.querySelector('[data-menu-screen="home"]')||document.getElementById('menu')||document.body; const button=make('button',{type:'button',id:'ka-online-leaderboards-open'},'ONLINE LEADERBOARDS'); home.append(button); const dialog=make('dialog',{id:'ka-online-leaderboards-dialog','aria-labelledby':'ka-online-lb-title'}); const shell=make('div',{class:'ka-online-lb-shell'}); const head=make('div',{class:'ka-online-lb-head'}); head.append(make('h2',{id:'ka-online-lb-title'},'ONLINE LEADERBOARDS'),make('button',{type:'button',id:'ka-online-lb-close',class:'ka-online-lb-close','aria-label':'Close online leaderboards'},'×')); const profile=make('div',{class:'ka-online-lb-profile'}); const nameInput=make('input',{id:'ka-online-lb-name',maxlength:'24','aria-label':'Leaderboard display name',placeholder:'Display name'}); const saveName=make('button',{type:'button',id:'ka-online-lb-save-name'},'SAVE NAME'); const retry=make('button',{type:'button',id:'ka-online-lb-retry'},'RETRY QUEUED '); retry.append(make('span',{id:'ka-online-lb-pending'},'0')); profile.append(nameInput,saveName,retry); const filters=make('div',{class:'ka-online-lb-filters'}); const scope=make('select',{id:'ka-online-lb-scope','aria-label':'Leaderboard scope'}); scope.append(make('option',{value:'global'},'Global'),make('option',{value:'region'},'My Region')); const map=make('select',{id:'ka-online-lb-map','aria-label':'Leaderboard map'}); ONLINE_LEADERBOARD_MAPS.forEach((item)=>map.append(make('option',{value:item.id},item.label))); const diff=make('select',{id:'ka-online-lb-difficulty','aria-label':'Leaderboard difficulty'}); ONLINE_LEADERBOARD_DIFFICULTIES.forEach((item)=>diff.append(make('option',{value:item.id},item.label))); const refreshButton=make('button',{type:'button',id:'ka-online-lb-refresh'},'REFRESH'); filters.append(scope,map,diff,refreshButton); const status=make('div',{id:'ka-online-lb-status',class:'ka-online-lb-status'},'READY'); const wrap=make('div',{class:'ka-online-lb-table-wrap'}); const table=make('table',{class:'ka-online-lb-table'}); const thead=make('thead'); const header=make('tr'); ['#','Player','Region','Score','Wave','Kills','Time','Accuracy','Date'].forEach((label)=>header.append(make('th',{},label))); thead.append(header); const tbody=make('tbody',{id:'ka-online-lb-body'}); table.append(thead,tbody); wrap.append(table); shell.append(head,profile,filters,status,wrap,make('p',{class:'ka-online-lb-note'},'Anonymous public rankings. Challenge-bound submissions, duplicate-run rejection, and offline retry are enabled. Local leaderboards remain available separately.')); dialog.append(shell); document.body.append(dialog); button.addEventListener('click',open); document.getElementById('ka-online-lb-close').addEventListener('click',close); dialog.addEventListener('cancel',(event)=>{event.preventDefault();close();}); dialog.addEventListener('click',(event)=>{if(event.target===dialog)close();}); document.getElementById('ka-online-lb-save-name').addEventListener('click',()=>{const input=document.getElementById('ka-online-lb-name'); const name=cleanOnlineDisplayName(input.value); input.value=name; writeStorage(DISPLAY_NAME_KEY,name); setStatus('DISPLAY NAME SAVED','pass');}); document.getElementById('ka-online-lb-retry').addEventListener('click',()=>void retryPending()); [scope,map,diff].forEach((element)=>element.addEventListener('change',()=>void refresh())); refreshButton.addEventListener('click',()=>void refresh()); updatePendingCount(); render(); }
export function initOnlineLeaderboards(){ if(uiBound)return; uiBound=true; buildUi(); document.documentElement.dataset.kaOnlineLeaderboards='ready'; void retryPending(); }
export function getOnlineLeaderboardSnapshot(){ return {activeRun:activeRun?{...activeRun,challengePromise:undefined}:null,pending:[...pending()],latest}; }
if(typeof window!=='undefined'){ window.KAGetOnlineLeaderboards=getOnlineLeaderboardSnapshot; window.KARetryOnlineLeaderboardSubmissions=retryPending; }
